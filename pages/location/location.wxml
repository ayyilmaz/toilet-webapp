<view>
  <map id="map" 
      longitude="{{longitude}}" 
      latitude="{{latitude}}" 
      scale="15" 
      markers="{{markers}}" 
      bindmarkertap="doMarkertap"
      polyline="{{polyline}}"
      show-location>
  </map>
</view>
